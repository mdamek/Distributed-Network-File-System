syntax = "proto3";

message Path {
    string path = 1;
}

message FolderContents {
    string path = 1;
    string folders = 2;
    string files = 3;
}

service  NFSServer {
    rpc ListDir(Path) returns (FolderContents) {}
}